diff --git a/out/fileMatcher.js b/out/fileMatcher.js
index ea2c790b033f6c1d6a6ec5cc99c7d09d42d82ff0..1b7f0220ef98226436f4d787f3d0e4c32ae18e02 100644
--- a/out/fileMatcher.js
+++ b/out/fileMatcher.js
@@ -200,7 +200,13 @@ function getNodeModuleFileMatcher(appDir, destination, macroExpander, platformSp
                 const fileSet = pattern;
                 if (fileSet.from == null || fileSet.from === ".") {
                     for (const p of (0, builder_util_1.asArray)(fileSet.filter)) {
-                        matcher.addPattern(p);
+                        // App file sets are normalized to object form before this
+                        // function runs. Only exclusions apply to production
+                        // dependencies; a positive app filter such as `out` would
+                        // otherwise exclude every collected package file.
+                        if (typeof p === "string" && p.startsWith("!")) {
+                            matcher.addPattern(p);
+                        }
                     }
                 }
             }
diff --git a/templates/nsis/multiUser.nsh b/templates/nsis/multiUser.nsh
index 3de631bf0ce5095d03062ba98a68c5249904911e..4af01297971cccda147b0e88e86364e074d53921 100644
--- a/templates/nsis/multiUser.nsh
+++ b/templates/nsis/multiUser.nsh
@@ -27,15 +27,7 @@ Var installMode
     ${if} $perUserInstallationFolder != ""
       StrCpy $INSTDIR $perUserInstallationFolder
     ${else}
-      StrCpy $0 "$LocalAppData\Programs"
-      System::Store S
-      # Win7 has a per-user programfiles known folder and this can be a non-default location
-      System::Call 'SHELL32::SHGetKnownFolderPath(g "${FOLDERID_UserProgramFiles}", i ${KF_FLAG_CREATE}, p 0, *p .r2)i.r1'
-      ${If} $1 == 0
-        System::Call '*$2(&w${NSIS_MAX_STRLEN} .s)'
-        StrCpy $0 $1
-        System::Call 'OLE32::CoTaskMemFree(p r2)'
-      ${endif}
-      System::Store L
-      StrCpy $INSTDIR "$0\${APP_FILENAME}"
+      # Avoid the legacy NSIS System.dll call to SHGetKnownFolderPath. It can
+      # access-violate on current Windows before the installer UI is created.
+      StrCpy $INSTDIR "$LocalAppData\Programs\${APP_FILENAME}"
     ${endif}
